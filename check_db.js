const mysql = require('mysql2/promise'); require('dotenv').config({ path: '.env.local' }); async function run() { try { const pool = mysql.createPool({ host: process.env.MYSQL_HOST, user: process.env.MYSQL_USER, password: process.env.MYSQL_PASSWORD, database: process.env.MYSQL_DATABASE }); const [tables] = await pool.query('SHOW TABLES'); console.log(tables); } catch(e) { console.error(e); } process.exit(0); } run();
