const mysql = require('mysql2/promise'); require('dotenv').config({ path: '.env.local' }); async function run() { try { const pool = mysql.createPool({ host: process.env.MYSQL_HOST, user: process.env.MYSQL_USER, password: process.env.MYSQL_PASSWORD, database: process.env.MYSQL_DATABASE }); const [cols1] = await pool.query('SHOW COLUMNS FROM family_links'); console.log('family_links', cols1); const [cols2] = await pool.query('SHOW COLUMNS FROM patient_family_links'); console.log('patient_family_links', cols2); } catch(e) { console.error(e); } process.exit(0); } run();
